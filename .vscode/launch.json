{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "debugAdapter":"dlv-dap",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/cmd",
            "cwd": "${workspaceRoot}"
        }
    ]
}
